## 2025-05-01 - Consistent CLI UX with @clack/prompts
**Learning:** Using `@clack/prompts` across all CLI commands (create, run) provides a more cohesive and professional feel through consistent intro/outro elements and styled inputs. Handling `isCancel` is crucial for graceful termination of interactive loops.
**Action:** Always prefer `@clack/prompts` for interactive CLI experiences in this repository and ensure `intro`, `outro`, and cancellation handling are implemented.
